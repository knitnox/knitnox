# knitnox (Svelte 5 + MCP)

A high-performance, lightweight, and completely browser-based LLM interface built with **Svelte 5**, **Tailwind CSS 4**, and **Dexie.js**. knitnox is designed for developers and power users who want a private, "Bring Your Own API" experience with first-class support for **MCP (Model Context Protocol)**.

---

## 🎨 UI/UX Philosophy

knitnox prioritizes a **clean, focused, and responsive** experience. 

### Core UI Characteristics:
- **Minimalist Aesthetic:** Inspired by modern chat interfaces, using a zinc/white color palette that respects system dark mode.
- **Immediate Feedback:** Leveraging Svelte 5's reactivity, every token streams into the UI with zero perceived lag.
- **Non-Intrusive Components:** Settings and navigation are kept in sidebars and modals, keeping the primary focus on the conversation.
- **Rich Interaction:** Supports complex data types through Markdown rendering and interactive "thought" processes for reasoning models.

---

## 🏗 Component Architecture & Functionality

### 1. **Sidebar (`Sidebar.svelte`)**
The navigation hub for session management.
- **Functions:**
    - `handleNewChat`: Resets the active session to start a fresh conversation.
    - `onSelectChat`: Switches between historical sessions stored in IndexedDB.
    - `deleteChat`: Permanently removes a session and its associated messages.
- **UX Detail:** Chat titles are automatically generated by the LLM after the first message to provide context at a glance.

### 2. **Chat Interface (`Chat.svelte`)**
The heart of the application, managing the conversation flow and tool execution.
- **Core Functions:**
    - `handleSubmit`: Orchestrates the sending of user messages, UI state updates, and triggers the title generation for new chats.
    - `processChat`: A sophisticated loop that manages the LLM request, tool discovery, execution of MCP tool calls, and result feedback.
    - `renderMarkdown`: Securely converts LLM responses into rich HTML with syntax highlighting.
    - `scrollToBottom`: An automated effect ensuring the latest response is always in view during streaming.
- **UI Highlights:**
    - **Expandable Thought Badge:** For reasoning models (like DeepSeek R1), an interactive badge shows the model's "internal monologue" with a precise duration timer (e.g., "Thought (4.5s)").
    - **Tool Status Indicators:** Visual cues when a message is "Using tool: [Name]" to keep the user informed of background actions.
    - **Loader States:** Smooth animations for "Thinking..." and token streaming.

### 3. **Settings Modal (`SettingsModal.svelte`)**
A comprehensive panel for persistent user configuration.
- **Key Settings:**
    - **Provider Config:** Base URL, API Key, and Model selection.
    - **Reasoning Toggle:** "Enable Thinking" switch to control whether the UI processes and displays reasoning tokens.
    - **System Prompt:** Global behavior definition.
    - **MCP Servers:** A dynamic list where users can add/remove SSE endpoints for tool integration.

---

## 🚀 Key Functionalities

### 1. **Advanced Streaming & Reasoning**
Unlike standard interfaces, knitnox understands **reasoning tokens**. It separates a model's "thinking" from its "final answer," allowing you to see the logic behind the response in an expandable, non-cluttered way.

### 2. **MCP (Model Context Protocol) Integration**
This application acts as a native MCP Client. It:
- **Discovers:** Automatically queries configured MCP servers for available tools.
- **Executes:** Transparently handles the cycle of: *User -> LLM -> Tool Call -> MCP Server -> Tool Result -> LLM -> Final Response*.
- **Syncs:** Keeps tool outputs stored in your local history.

### 3. **Markdown & Syntax Highlighting**
- **Rich Text:** Supports bold, italics, tables, and lists via `marked`.
- **Code Highlighting:** Integrated with `highlight.js` (GitHub Dark theme) for professional-grade code blocks.
- **Security:** Every piece of rendered HTML is sanitized through `DOMPurify` to prevent XSS attacks.

### 4. **Local-First Privacy**
- **IndexedDB (Dexie.js):** All chat history is stored locally in your browser. No backend server ever sees your data.
- **Local Storage:** Your API keys and settings are stored locally, encrypted by the browser's own security model.
- **No Tracking:** Zero telemetry or external tracking.

---

## 🛠 Project Structure

- `src/lib/settings.svelte.ts`: Uses Svelte 5 **Runes** (`$state`) to manage global settings with automatic `localStorage` synchronization.
- `src/lib/db.ts`: Defines the IndexedDB schema for `chats` and `messages`.
- `src/lib/openai.svelte.ts`: A robust wrapper around the OpenAI SDK that handles both standard completions and the unique streaming requirements of OpenRouter/Reasoning models.
- `src/lib/mcp.svelte.ts`: Implements the SSE/HTTP transport layer for the Model Context Protocol.

---

## 📝 Getting Started

1. **Install Dependencies:** `npm install`
2. **Run Dev Server:** `npm run dev`
3. **Configure:** Open Settings, enter your API details and an MCP server URL (if available).
4. **Chat:** Start conversing with local or cloud-based AI.

---

## 📝 License

MIT
e

MIT
